// custom hook must start with use and format shoudk be js its recomended jsx also work btw
import React from "react";

const CustomHook = () => {
  return <div>CustomHook</div>;
};

export default CustomHook;
